import styled from 'styled-components';

import background from 'assets/tazo-reverse.png';

export const StyledReverse = styled.div`
  border-radius: 100%;
  background-image: url(${background});
  background-size: contain;
  width: 100%;
  height: 100%;
`;
