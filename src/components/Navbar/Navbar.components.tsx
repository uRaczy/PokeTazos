import {
  StyledNavbar,
  StyledLogo,
  StyledMenu,
  StyledMenuItem,
} from './Navbar.styles';

const Navbar = () => (
  <StyledNavbar>
    <StyledLogo />
    <StyledMenu>
      <StyledMenuItem />
    </StyledMenu>
  </StyledNavbar>
);

export default Navbar;
